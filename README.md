# Task_4-by-skillsoft
Designed a Java Swing-based Keyboard Event Analyzer that records keys typed within the application window and saves them to a text file. The project demonstrates keyboard event handling, GUI development, and file operations while ensuring input is captured only inside the application.
